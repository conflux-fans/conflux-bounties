// Re-exported from @cfxdevkit/sdk — single source of truth.
export type {
  BaseJob,
  DCAJob,
  DCAParams,
  Job,
  JobStatus,
  JobType,
  LimitOrderJob,
  LimitOrderParams,
} from '@cfxdevkit/sdk/automation';
